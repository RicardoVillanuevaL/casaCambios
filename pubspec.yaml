name: application_casa_cambios
description: Aplicacion para selección, que trata de una casa de cambios.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animate_do: ^1.7.5
  flushbar: ^1.5.0
  cupertino_icons: ^1.0.0
  firebase_auth: null
  firebase_core: ^0.5.0+1
  firebase_database: null
  firebase_messaging: ^7.0.3
  google_fonts: ^1.1.2
  http: ^0.12.2
  provider: ^4.3.3
  shared_preferences: ^0.5.12+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/logos/
